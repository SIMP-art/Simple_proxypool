# Simple ProxyPool aka PP ;p

This is just script that gather proxy list from another github repo or site.

## Usage

Use [golang](https://go.dev/) to run script by:

```bash
go run main.go
```
or build it by :
```bash
go build main.go
```
To do :
```
Checker
> Proxy checker
> Country filter
> Check timeout
```
